accelerate launch \
    --num_machines 4 \
    --num_processes 32 \
    --use_deepspeed \
    --deepspeed_multinode_launcher 'standard' \
    --zero_stage 2 \
    --offload_optimizer_device 'cpu' \
    --offload_param_device 'none' \
    --gradient_accumulation_steps 1 \
    --gradient_clipping 1.0 \
    --zero3_init_flag false \
    --zero3_save_16bit_model false \
    --main_training_function 'main' \
    --mixed_precision 'bf16' \
    --dynamo_backend 'no' \
    --same_network \
    --machine_rank $RANK \
    --main_process_ip $MASTER_ADDR \
    --main_process_port $MASTER_PORT \
    --rdzv_backend 'static' \
    llava/train/train_mem.py \
    --data_path /your/train/data/path \
    --eval_path /your/eval/data/path \
    --is_parquet True \
    --output_dir /path/to/save/weights \
    --image_folder /path/to/store/images/ \
    --image_aspect_ratio None \
    --lora_enable True \
    --lora_r 16 \
    --lora_alpha 64 \
    --model_name_or_path /path/to/model/ \
    --version qwen \
    --vision_tower /path/to/visiontower/ \
    --mm_projector_type cross_attn \
    --pretrain_mm_mlp_adapter /path/to/mmprojector/mm_projector.bin \
    --mm_vision_select_layer -2 \
    --mm_use_im_start_end False \
    --use_im_start_end False \
    --mm_use_im_patch_token False \
    --bf16 True \
    --num_train_epochs 5 \
    --per_device_train_batch_size 4 \
    --per_device_eval_batch_size 2 \
    --gradient_accumulation_steps 1 \
    --evaluation_strategy "steps" \
    --eval_steps 1000 \
    --load_best_model_at_end True \
    --save_strategy "steps" \
    --save_steps 1000 \
    --save_total_limit 10 \
    --freeze_backbone True \
    --learning_rate 2e-5 \
    --weight_decay 0. \
    --warmup_ratio 0.03 \
    --lr_scheduler_type "cosine" \
    --logging_steps 1 \
    --tf32 True \
    --model_max_length 2048 \
    --gradient_checkpointing True \
    --lazy_preprocess True \
    --dataloader_num_workers 4 \
    --report_to none